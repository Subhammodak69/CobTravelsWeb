import { useEffect } from "react";
import { useTravel } from "../contexts/TravelContext";
import usePackages from "../hooks/usePackages";
import useScrollReveal from "../hooks/useScrollReveal";
import PackageGallery from "../components/PackageGallery";
import Reviews from "../components/Reviews";
import { image } from "../data/packages";

export default function PackageDetailsPage() {
  const { selectedPackageId, returnToJourneys, isMember } = useTravel();
  const { getPackageById, reviews, posts } = usePackages();
  const pack = getPackageById(selectedPackageId); const revealRef = useScrollReveal();
  useEffect(() => {
    window.scrollTo({ top: 0, behavior: "smooth" });
  }, [selectedPackageId]);
  if (!pack) return null;
  return <div ref={revealRef}><section className="detailHero" style={{ backgroundImage: `linear-gradient(90deg, rgba(12,20,19,.78), rgba(12,20,19,.08)), url(${pack.image})` }}><button className="back" onClick={returnToJourneys}>← &nbsp; All journeys</button><div className="detailTitle"><p>{pack.code} · {pack.duration}</p><h1>{pack.title}</h1><div><span><span className="star">★</span> 4.8 <small>(128 traveller reviews)</small></span><span>From <b>₹{pack.price.toLocaleString("en-IN")}</b> / person</span></div></div><div className="scrollTag">SCROLL TO EXPLORE <i>↓</i></div></section><section className="facts"><div><label>Journey route</label><p>{pack.route.join(" · ")}</p></div><div><label>Style</label><p>3 Star Deluxe</p></div><div><label>Best for</label><p>Couples · Families · Friends</p></div><button className="reserve">{isMember ? "Reserve your place" : "Plan this journey"} <span>→</span></button></section><section className="overview reveal" data-reveal><div className="overviewIntro"><p className="eyebrow">The experience</p><h2>Follow the feeling,<br/><em>not the crowd.</em></h2><p>Every day on this itinerary balances iconic moments with time to breathe. This is travel with the details taken care of.</p><div className="highlights">{pack.highlights.map((highlight) => <span key={highlight}>✦ &nbsp;{highlight}</span>)}</div></div><div className="videoBlock"><video poster={pack.image} controls preload="metadata"><source src="https://interactive-examples.mdn.mozilla.net/media/cc0-videos/flower.mp4" type="video/mp4"/></video><div className="videoCaption">A glimpse of the journey <span>01:00</span></div></div></section><div className="reveal" data-reveal><PackageGallery pack={pack}/></div><section className="itinerary reveal" data-reveal><p className="eyebrow">A day-by-day rhythm</p><h2>The route unfolds<br/><em>beautifully.</em></h2><div className="days">{pack.itinerary.map(([title, description], index) => <article key={title}><span>Day {String(index + 1).padStart(2, "0")}</span><div><h3>{title}</h3><p>{description}</p></div><b>+</b></article>)}</div></section><section className="departures reveal" data-reveal><div><p className="eyebrow">Upcoming departures</p><h2>Pick your<br/><em>perfect moment.</em></h2></div><div className="dateContent"><p><b>{pack.season}</b><br/>Select a preferred departure date and our team will confirm availability within one working day.</p><div>{pack.dates.map((date, index) => <button key={date} className={index === 0 ? "date activeDate" : "date"}>{date}</button>)}</div><button className="reserve dark">Check availability <span>→</span></button></div></section><div className="reveal" data-reveal><Reviews reviews={reviews}/></div><section className="journal reveal" data-reveal><div className="sectionHead"><div><p className="eyebrow">From the journal</p><h2>Travel notes &<br/><em>good advice.</em></h2></div></div><div className="postGrid">{posts.map((post, index) => <article key={post}><img src={image(pack.gallery[(index + 1) % pack.gallery.length], 700, 440)} alt=""/><p>TRAVEL NOTES · 4 MIN READ</p><h3>{post}</h3><button>Read story →</button></article>)}</div></section></div>;
}
