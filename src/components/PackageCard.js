import { useTravel } from "../contexts/TravelContext";
export default function PackageCard({ pack, index }) { const { selectPackage } = useTravel(); return <article className="packageCard" style={{ "--accent": pack.accent }} onClick={() => selectPackage(pack.id)}><div className="cardImage"><img src={pack.image} alt={pack.title}/><span className="cardNo">0{index + 1}</span><button aria-label={`Explore ${pack.title}`}>↗</button></div><div className="cardBody"><p>{pack.code} <span>·</span> {pack.duration}</p><h3>{pack.title}</h3><div className="cardBottom"><span>From <b>₹{pack.price.toLocaleString("en-IN")}</b></span><span>{pack.route.slice(0, 2).join(" · ")}</span></div></div></article>; }
