import { useState } from "react";
import { image } from "../data/packages";
export default function PackageGallery({ pack }) { const [activeImage, setActiveImage] = useState(0); return <section className="gallerySection"><div className="sectionHead"><div><p className="eyebrow">Through our lens</p><h2>Scenes worth<br/>remembering.</h2></div><p>Swipe through a small taste of the colours, textures and moments waiting for you.</p></div><div className="gallery"><img className="mainGalleryImage" src={image(pack.gallery[activeImage])} alt={`${pack.title} gallery`}/><div className="galleryRail">{pack.gallery.map((photo, index) => <button className={index === activeImage ? "active" : ""} onClick={() => setActiveImage(index)} key={`${photo}-${index}`}><img src={image(photo, 300, 200)} alt=""/></button>)}</div></div></section>; }
